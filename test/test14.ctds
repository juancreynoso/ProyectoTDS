Program {
    integer x = 10;
    void assert(integer n, integer m) extern;
    integer main() {
        x = 1;
        integer r = x + 3;
        assert(r, 4);
        return r;

        x = 2;
        if (x ==3)then {
            x = 2;
        }
        return 2;
    }
    
}