Program
{
    integer print(integer a) extern;
    void assert(integer n, integer m) extern;

    integer main() {
        integer y = 1;
        integer x = 0;
        x = y + 3;
        y = x * 2;

        assert(y, 8);
        print(y);
        return y;
    }
}